// NOTE: this constants are use in the client side only

export const mediumPrice = Number(process.env.NEXT_PUBLIC_MEDIUM_CREDIT_COST)
export const highPrice = Number(process.env.NEXT_PUBLIC_HIGH_CREDIT_COST)
export const lowPrice = Number(process.env.NEXT_PUBLIC_LOW_CREDIT_COST)
